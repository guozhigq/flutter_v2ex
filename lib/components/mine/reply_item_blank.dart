import 'package:flutter/material.dart';

class ReplyItemBlank extends StatelessWidget {
  const ReplyItemBlank({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
