import 'package:flutter/material.dart';

class TopicItemBlank extends StatelessWidget {
  const TopicItemBlank({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
